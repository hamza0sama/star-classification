# star-classification
This project implements a complete machine learning pipeline for classification using Support Vector Machines (SVM). It includes data preprocessing , Principal Component Analysis (PCA) , exploratory data analysis (EDA), feature engineering, dimensionality reduction, model training, and evaluation.
